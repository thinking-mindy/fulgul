name: Build Windows

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      source_branch:
        description: 'Source branch to build from'
        required: false
        default: 'main'

jobs:
  build-windows-x64:
    name: Build Windows 64-bit
    runs-on: windows-latest
    
    steps:
      - name: Checkout source code from fulgul-source
        uses: actions/checkout@v4
        with:
          repository: thinking-mindy/fulgul-source
          ref: ${{ github.event.inputs.source_branch || 'main' }}
          token: ${{ secrets.SOURCE_REPO_TOKEN }}
          path: .

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Rust (x86_64-pc-windows-msvc)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Install dependencies
        run: npm install

      - name: Build frontend
        run: npm run build

      - name: Build Tauri app for Windows 64-bit (NSIS Installer)
        run: npm run tauri:build:win
        env:
          TAURI_PRIVATE_KEY: ${{ secrets.TAURI_PRIVATE_KEY }}
          TAURI_KEY_PASSWORD: ${{ secrets.TAURI_KEY_PASSWORD }}

      - name: Upload Windows 64-bit NSIS Installer
        uses: actions/upload-artifact@v4
        with:
          name: fulgul-windows-x64-nsis
          path: src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/*.exe
          retention-days: 30

      - name: Upload Windows 64-bit Standalone Executable
        uses: actions/upload-artifact@v4
        with:
          name: fulgul-windows-x64-exe
          path: src-tauri/target/x86_64-pc-windows-msvc/release/fulgul.exe
          retention-days: 30

      - name: Create Release (on tag)
        if: startsWith(github.ref, 'refs/tags/')
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          body: |
            ## 🚀 Fulgul ${{ github.ref_name }}
            
            ### Windows Builds
            - **NSIS Installer**: Ready-to-install Windows installer
            - **Standalone Executable**: Portable executable file
            
            ### Installation
            1. Download the NSIS installer (`.exe`) for the easiest installation
            2. Or download the standalone executable for portable use
            
            ---
            *Automatically built and released via GitHub Actions*
          files: |
            src-tauri/target/x86_64-pc-windows-msvc/release/bundle/nsis/*.exe
            src-tauri/target/x86_64-pc-windows-msvc/release/fulgul.exe
          draft: false
          prerelease: ${{ contains(github.ref_name, 'alpha') || contains(github.ref_name, 'beta') || contains(github.ref_name, 'rc') }}
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
